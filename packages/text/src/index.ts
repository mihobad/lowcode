import { TextClient } from './client';
import { textPropsConfig, textDataConfig, textInit } from './merchant';
import { textSchemaJson } from './schema';

export { TextClient, textPropsConfig, textDataConfig, textInit, textSchemaJson };

export default TextClient;
