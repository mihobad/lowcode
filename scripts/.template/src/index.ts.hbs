import { {{pascalCase name}}Client } from './client';
import { {{camelCase name}}PropsConfig, {{camelCase name}}DataConfig, {{camelCase name}}Init } from './merchant';
import { {{camelCase name}}SchemaJson } from './schema';

export { {{pascalCase name}}Client, {{camelCase name}}PropsConfig, {{camelCase name}}DataConfig, {{camelCase name}}Init, {{camelCase name}}SchemaJson };

export default {{pascalCase name}}Client;